namespace frc.taurus.config.generated;

// Every entry in this enumeration also needs to be added to the Config enumeration

enum ChannelType : short {
  DriverStationStatus,
  JoystickStatus, 
  JoystickGoal,
  }

